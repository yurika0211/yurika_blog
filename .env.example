# Blog Stack Environment Template
# Usage:
#   cp .env.example .env
#   edit .env and fill secrets

# ---------- Database (required) ----------
POSTGRES_USER=admin
POSTGRES_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD
POSTGRES_DB=postgres
DB_PORT=5433
DATABASE_URL=postgres://admin:CHANGE_ME_TO_A_STRONG_PASSWORD@localhost:5433/postgres

# ---------- Authentication (required) ----------
# Generate with: openssl rand -base64 48
JWT_SECRET=CHANGE_ME_TO_A_RANDOM_SECRET_AT_LEAST_32_BYTES
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGE_ME_TO_A_UNIQUE_PASSWORD_AT_LEAST_12_CHARS
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGE_ME_TO_A_UNIQUE_PASSWORD_AT_LEAST_12_CHARS

# ---------- Frontend (build args / runtime) ----------
# Docker/production frontend usually talks to the reverse proxy paths below.
VITE_API_BASE_URL=/api
VITE_CHAT_API_BASE_URL=/api/v1

# Host port -> frontend container port 80 (docker-compose.yml)
FRONTEND_PORT=3000

# Local Vite dev proxy target (frontend/vite.config.ts)
VITE_CHAT_PROXY_TARGET=http://127.0.0.1:8080

# ---------- Chat AI (required) ----------
# Make sure OPENAI_API_URL and OPENAI_MODEL match the same provider.
# OpenAI example:
#   OPENAI_API_URL=https://api.openai.com/v1/chat/completions
#   OPENAI_MODEL=gpt-4o-mini
# DeepSeek example:
#   OPENAI_API_URL=https://api.deepseek.com/chat/completions
#   OPENAI_MODEL=deepseek-chat
OPENAI_API_KEY=replace_with_real_key
OPENAI_API_URL=https://api.openai.com/v1/chat/completions
OPENAI_MODEL=gpt-4o-mini
SYSTEM_CONTENT="You are a helpful AI assistant."
CHAT_PORT=8080

# ---------- RAG (optional) ----------
RAG_ENABLED=false
RAG_WRITE_ENABLED=true
OPENAI_EMBEDDING_URL=https://api.openai.com/v1/embeddings
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
RAG_TOP_K=6

# ---------- Memory nudge (optional) ----------
MEMORY_NUDGE_ENABLED=true
MEMORY_NUDGE_PROB=0.20
MEMORY_NUDGE_COOLDOWN_TURNS=5
MEMORY_NUDGE_EXCLUDE_RECENT=8
MEMORY_NUDGE_MAX_CHARS=160
